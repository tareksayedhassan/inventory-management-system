         <div className="grid grid-cols-1 lg:grid-cols-3 gap-6 pt-4 items-start">
                 <div className="lg:col-span-1 space-y-4">
                    <div>
                        <label className="block mb-2 text-sm font-medium">نسبة الضريبة (%)</label>
                        <input type="number" value={taxRate} onChange={e => setTaxRate(parseFloat(e.target.value) || 0)} className={INPUT_CLASSES} disabled={!isEditMode} />
                    </div>
                     <div>
                        <label className="block mb-2 text-sm font-medium">خصم المنبع (%)</label>
                        <input type="number" value={withholdingTaxRate} onChange={e => setWithholdingTaxRate(parseFloat(e.target.value) || 0)} className='bg-white dark:bg-gray-700 border border-gray-300 dark:border-gray-600 text-gray-900 dark:text-white text-sm rounded-md focus:ring-primary-500 focus:border-primary-500 block w-full p-2.5 transition-colors disabled:bg-gray-200 dark:disabled:bg-gray-600/50 disabled:cursor-not-allowed' disabled={!isEditMode}/>
                    </div>
                </div>
                 <div className="lg:col-span-2">
                    <div className="grid grid-cols-2 gap-4">
                        <div className="bg-white dark:bg-gray-700/50 p-3 rounded-lg text-center shadow-md border border-gray-200 dark:border-gray-700">
                            <p className="text-sm text-gray-500 dark:text-gray-400">الإجمالي الفرعي</p>
                            <p className="text-xl font-bold font-numeric text-gray-800 dark:text-white">{formatCurrency(calculations.subtotal)}</p>
                        </div>
                        <div className="bg-white dark:bg-gray-700/50 p-3 rounded-lg text-center shadow-md border border-gray-200 dark:border-gray-700">
                            <p className="text-sm text-gray-500 dark:text-gray-400">الضريبة ({taxRate}%)</p>
                            <p className="text-xl font-bold font-numeric text-gray-800 dark:text-white">{formatCurrency(calculations.taxAmount)}</p>
                        </div>
                         {type === 'sale' && (
                            <div className="bg-white dark:bg-gray-700/50 p-3 rounded-lg text-center shadow-md border border-gray-200 dark:border-gray-700">
                                <p className="text-sm text-gray-500 dark:text-gray-400">صافي ضريبة القيمة المضافة</p>
                                <p className={`text-xl font-bold font-numeric ${calculations.netTaxAmount >= 0 ? 'text-green-600' : 'text-red-600'}`}>{formatCurrency(calculations.netTaxAmount)}</p>
                            </div>
                        )}
                        <div className="bg-white dark:bg-gray-700/50 p-3 rounded-lg text-center shadow-md border border-gray-200 dark:border-gray-700">
                            <p className="text-sm text-gray-500 dark:text-gray-400">خصم المنبع ({withholdingTaxRate}%)</p>
                            <p className="text-xl font-bold font-numeric text-red-500">-{formatCurrency(calculations.withholdingTaxAmount)}</p>
                        </div>
                    </div>
                    <div className="bg-primary-600 text-white p-4 rounded-lg text-center shadow-lg mt-4">
                        <p className="text-lg font-semibold">الإجمالي النهائي</p>
                        <p className="text-3xl font-bold font-numeric tracking-wider">{formatCurrency(calculations.totalAmount)}</p>
                    </div>
                </div>
            </div>
            